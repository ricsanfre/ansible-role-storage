[defaults]
# Inventory file location
inventory       = ./inventory
# Ansible execution threads
forks          = 5
# Paths to search for roles in, colon separated
roles_path    = $HOME/repo
# Disable SSH key host checking
host_key_checking = false

